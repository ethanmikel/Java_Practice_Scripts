
/**
 * Write a description of class test here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class test
{
   public static String changeKey(String letter, String key) {
 String let = letter;
 if (let.eqauls(letter)) {
   int place = key.indexOf(key, let);
   String n = String.replace(place, "");
 }
 return n;
}
}
